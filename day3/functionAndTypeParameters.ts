// function and types Parameters
// here we can define function parameter typer whatever it is string or Number etc
function sum(a: number, b: number): number {
  return a + b;
}
let res6 = sum(2, 5);
console.log(res6);
