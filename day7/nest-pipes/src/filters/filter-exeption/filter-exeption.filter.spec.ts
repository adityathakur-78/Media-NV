import { FilterExeptionFilter } from './filter-exeption.filter';

describe('FilterExeptionFilter', () => {
  it('should be defined', () => {
    expect(new FilterExeptionFilter()).toBeDefined();
  });
});
